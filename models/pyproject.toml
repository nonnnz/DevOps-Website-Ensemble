[project]
name = "datathai-local-server"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.29",
    "transformers>=4.40",
    "torch>=2.2",
    "safetensors>=0.4",
    "pydantic>=2.0",
]

[project.scripts]
serve = "serve_local:main"
